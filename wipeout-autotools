#!/bin/sh

/bin/rm -rf missing ltmain.sh install-sh  depcomp configure config.sub config.guess autom4te.cache aclocal.m4 INSTALL compile
/bin/rm -rf Makefile.in apps/Makefile.in autoscan.log config.h.in tuning/Makefile.in

# Do autoreconf -i to recover.
